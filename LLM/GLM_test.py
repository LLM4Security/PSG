import os
import platform
import torch
from transformers import AutoTokenizer, AutoModel
import json
from tqdm import tqdm
import time

tokenizer = AutoTokenizer.from_pretrained("/home/jiuyan/chatglm3-6b", trust_remote_code=True)
model = AutoModel.from_pretrained("/home/jiuyan/chatglm3-6b", trust_remote_code=True, device='cuda')
model = model.eval()
start_time = time.time()
response, history = model.chat(tokenizer, "code segment:\n@Override // android.view.View$OnClickListener\npublic void onClick(android.view.View v) {\n    android.widget.EditText email;\n    android.widget.EditText password;\n    android.widget.LinearLayout body = new android.widget.LinearLayout(this.this$0);\n    body.setOrientation(1);\n    body.setPadding(10, 0, 10, 0);\n    email = this.this$0.addEditField(body, com.timsu.astrid.R$string.actfm_ALA_email_label);\n    email.setInputType(33);\n    this.this$0.getCredentials(new com.todoroo.astrid.actfm.ActFmLoginActivity$6$1(this, email));\n    password = this.this$0.addEditField(body, com.timsu.astrid.R$string.actfm_ALA_password_label);\n    password.setInputType(129);\n    password.setTransformationMethod(new android.text.method.PasswordTransformationMethod());\n    android.widget.TextView forgotPassword = new android.widget.TextView(this.this$0);\n    android.text.SpannableString text = new android.text.SpannableString(this.this$0.getString(com.timsu.astrid.R$string.actfm_ALA_forgot_password));\n    text.setSpan(new android.text.style.UnderlineSpan(), 0, text.length(), 0);\n    forgotPassword.setText(text);\n    android.widget.LinearLayout$LayoutParams lp = new android.widget.LinearLayout$LayoutParams(-2, -2);\n    android.util.DisplayMetrics metrics = this.this$0.getResources().getDisplayMetrics();\n    lp.setMargins(0, (int) (metrics.density * 8.0f), 0, (int) (metrics.density * 8.0f));\n    forgotPassword.setLayoutParams(lp);\n    forgotPassword.setOnClickListener(new com.todoroo.astrid.actfm.ActFmLoginActivity$6$2(this, email));\n    body.addView(forgotPassword);\n    android.widget.ScrollView bodyScroll = new android.widget.ScrollView(this.this$0);\n    bodyScroll.addView(body);\n    new android.app.AlertDialog$Builder(this.this$0).setView(bodyScroll).setIcon(com.timsu.astrid.R$drawable.icon_32).setTitle(com.timsu.astrid.R$string.actfm_ALA_login_title).setPositiveButton(17039370, new com.todoroo.astrid.actfm.ActFmLoginActivity$6$4(this, email, password)).setNegativeButton(17039360, new com.todoroo.astrid.actfm.ActFmLoginActivity$6$3(this, email, password)).show();\n}\nWhat is the purpose of the code segment?", history=[{'role':'user' ,'content':'code segment:\n@Override // android.content.DialogInterface.OnClickListener\npublic void onClick(DialogInterface dlg, int which) {\n    String firstName = firstNameField.getText().toString();\n    String lastName = lastNameField.getText().toString();\n    AndroidUtilities.hideSoftInputForViews(ActFmLoginActivity.this, firstNameField, lastNameField, email);\n    ActFmLoginActivity.this.authenticate(email.getText().toString(), firstName, lastName, ActFmInvoker.PROVIDER_PASSWORD, ActFmLoginActivity.this.generateRandomPassword());\n    StatisticsService.reportEvent(StatisticsConstants.ACTFM_SIGNUP_PW, new String[0]);\n}\nWhat is the purpose of the code segment?'},{'role': 'assistant', 'metadata': '', 'content': 'When the button in the dialog is clicked, a progress dialog will be created, and a series of operations will be executed in a new thread. These operations include rebuilding all synchronized data, completing the login operation on the UI thread, and finally closing the progress dialog.'}])
print(time.time()-start_time)
print(response)